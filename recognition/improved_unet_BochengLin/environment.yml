name: unet3d
channels:
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      # Core Dependencies
      - torch==2.6.0+cu124
      - torchvision==0.21.0+cu124
      - numpy>=1.19.0
      
      # Medical Imaging
      - nibabel>=3.2.0
      
      # Utilities
      - tqdm>=4.50.0
      - matplotlib>=3.3.0
      - pillow>=11.0.0
      - scikit-image>=0.25.2
      
      # Optional (for better visualization)
      - scipy>=1.15.3
      - opencv-python>=4.12.0.88
